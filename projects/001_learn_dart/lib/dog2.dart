class Dog2 {
  String name;
  int age;
  // String message;

  Dog2(this.name, this.age);

  // set greeting(String text) => message = "$name: $text";

  String get introduction => "my name is $name";
}
